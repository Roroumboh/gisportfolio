# Initialize Git
git init
git add index.html
git commit -m "Initial GIS portfolio commit"

# Add remote (replace YOURUSERNAME with your GitHub username)
git remote add origin https://github.com/YOURUSERNAME/gis-portfolio.git

# Push to GitHub
git branch -M main
git push -u origin main
